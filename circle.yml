machine:
  java:
    version: oraclejdk8
  environment:
    TERM: dumb
dependencies:
  pre:
      - echo y | (android -s update sdk -u -t "tools" || true)
      - echo y | (android -s update sdk -u -t "build-tools-23.0.3,android-23" || true)
      - echo y | (android -s update sdk -u -t "extra-android-m2repository" || true)
test:
  override:
    - ./gradlew --info check
